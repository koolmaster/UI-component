import styled from 'styled-components';

export const StyledAccordion = styled.div`
  display: flex;
  flex-direction: column;
  gap: 20px;
  .title {
    color: #333;
    font-size: 2em;
    font-weight: bold;
  }
`;
